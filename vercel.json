{
  "buildCommand": "node scripts/prep-db.js && npx prisma generate && (npx prisma db push --accept-data-loss || echo 'DB push failed, continuing...') && vite build",
  "installCommand": "npm install",
  "framework": "vite",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "handle": "filesystem"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}